#pragma once
#include <vector>
using namespace std;
class Solution {
public:
	/**
	* Returns a running sum of heights for each block dropped
	*/
	vector<int> fallingSquares(vector<pair<int, int>>& positions);
};

